import { ButtonProps } from '@mui/material/Button';
import { LinkProps } from 'react-router-dom';

/** The {@link default | ButtonNavLink} properties type. */
export type Props = ButtonProps<'a', LinkProps>;
